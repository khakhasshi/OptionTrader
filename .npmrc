# Project-local npm cache. The shared ~/.npm/_cacache contains files owned by
# another user (legacy sudo-npm), which breaks installs with EACCES. Keeping the
# cache in-tree sidesteps that without touching the global cache. See ASSUMPTIONS.md.
cache=.npm-cache
